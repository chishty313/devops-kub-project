# DEPRECATED — kept only to avoid breaking links in older docs.
#
# The current host-side reverse proxy is `host-nginx-stream.conf` in this
# same directory, which uses the nginx `stream {}` module to do pure
# Layer-4 TCP passthrough so cert-manager can terminate TLS *inside* the
# cluster.
#
# If you're looking at this file, you want the other one:
#
#     k8s/host-nginx-stream.conf
#
# and the install script:
#
#     scripts/25-setup-host-nginx.sh
